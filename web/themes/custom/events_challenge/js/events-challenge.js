/**
 * @file
 * Events Challenge behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.eventsChallenge = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
